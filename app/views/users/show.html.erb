<h1 class="text-center mt-5 text-white"><%= @user.username %>'s profile</h1>
<%= gravatar_for @user, size: 200 %>

<div class="text-center mt-4">
    <%= link_to "Edit your profile", edit_user_path(@user), class: "btn btn-outline-warning" %>
</div>

<h3 class="text-center mt-5 text-white">Articles</h3>

<div class="custom_pagination">
    <%= will_paginate @articles, :container => false %>
</div>

<%= render 'articles/article' %>

<div class="custom_pagination mb-5">
    <%= will_paginate @articles, :container => false %>
</div>